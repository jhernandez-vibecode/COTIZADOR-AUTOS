# Sub-páginas en línea clara — mockups aprobados (10 sep 2026)

Capturas de los mockups de `/polizas-activas/`, `/renovaciones/`, `/cancelacion/` y `/marcas-recargo/` que aprobó JC ("Dale, implementalo igual que las otras") antes de implementarlos sobre `css/linea-clara-consola.css` (tag `pre-subpaginas-linea-clara-10sep`).

Numeradas en el orden del recorrido: Póliza activa (01-04), Renovación confirmada (05-08, incluida la variante Solo WhatsApp), Cancelación (09 vacía, 10 con resultado) y Marcas (11); la 12 es móvil a 375 px con Póliza y Marcas lado a lado.

Datos de ejemplo del PDF de muestra y nombres inventados. No hay datos de clientes reales. En Renovación la vista previa del correo sale vacía en el mockup porque necesita un comprobante real.
